# Attention From Scratch

This repository contains my attempt at implementing the attention mechanism from scratch.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```
